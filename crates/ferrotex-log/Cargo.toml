[package]
name = "ferrotex-log"
version = "0.9.0"
edition = "2024"
description = "FerroTeX build log parser and event model"
license = "Apache-2.0 OR MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
